PORT=8080

# Cloud SQL(MySQL) credentials
DB_USER=app_user
DB_PASSWORD=change_me
DB_NAME=flexai

# Option 1) Cloud Run + Cloud SQL (Unix socket)
INSTANCE_CONNECTION_NAME=project-id:region:instance-name
DB_SOCKET_PATH=/cloudsql

# Option 2) Direct TCP connection (local or VM)
# DB_HOST=127.0.0.1
# DB_PORT=3306
