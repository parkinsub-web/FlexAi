PORT=8080

# MySQL credentials
DB_USER=app_user
DB_PASSWORD=change_me
DB_NAME=my_app

# VM 로컬 MySQL (기본)
DB_HOST=127.0.0.1
DB_PORT=3306

# Cloud Run + Cloud SQL (Unix socket) — 필요 시 활성화
# INSTANCE_CONNECTION_NAME=project-id:region:instance-name
# DB_SOCKET_PATH=/cloudsql
